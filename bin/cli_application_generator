#!/usr/bin/env ruby 

$:.unshift( File.join( File.dirname( __FILE__ ), %w[.. lib] ) )

require 'loader'

CLIAppGenerator::CliApplication.new( 
  Choice.choices[:application], 
  Choice.choices[:output],
  Choice.choices[:skell] ).run
